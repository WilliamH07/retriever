# retriever_bringup

Launch files and system configuration. The entry point is
`retriever.launch.py`, which takes a `use_sim` argument so that the whole stack
can be run against a simulated robot before any hardware exists.
